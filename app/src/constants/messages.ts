/**
 * 自分の言葉(KatsuMessage)の制限。
 *
 * 無料で書けるのは1つだけ。2つ目以降と、項目ごとの出し分けが有料(spec §2 課金)。
 * ⚠ これは「書き足せる件数」であって「保持できる件数」ではない。
 *   課金が切れても2つ目以降を削除しない。ロック表示にして再課金で復活させる。
 */

export const FREE_MESSAGE_LIMIT = 1;

/**
 * 1つの言葉の最大文字数。**コードポイント単位で数える**(`src/lib/text.ts`)。
 *
 * 40では英語が窮屈になり、80では二文の説教が書けてしまう。
 * 「一撃であること」を保ちつつ英語でも成立する幅が60(spec §2 文言の制約)。
 * 登録上限8件と同じく、制限そのものがメッセージになる。
 */
export const MESSAGE_MAX_LENGTH = 60;
