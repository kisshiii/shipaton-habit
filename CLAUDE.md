# KATSU — Claude Code 向けプロジェクト指示

このファイルはClaude Codeが毎回読む。**ここに書かれた制約を破らないこと。**

---

## プロジェクトの前提

- **iOSのみ。** Expo (React Native) SDK 57 + RevenueCat
- **バックエンドなし。** ネットワーク通信はRevenueCatのSDKのみ
- **8/29までにApp Store審査提出。** 開発者は1人、平日夜と土日のみ稼働
- 詳細仕様は `docs/spec.md`。**作業前に必ず読むこと**

---

## 絶対に守るルール

### スコープ

- **1.0の画面は3つだけ:** Routine setup / Today / Your KATSU
- **実装しないもの:** 統計画面、履歴一覧、複数目標、曜日指定、ソーシャル、 AI提案、卒業証書、クラウド同期、アカウント機能
- **新機能を提案しないこと。** 良いアイデアが浮かんでも9月以降の論点。 8/29までは決まったものを作りきることだけが正しい

### プライバシー・安全

- **ユーザーが書いた文言を端末外に送信しない。** ログにも出さない
- 自傷・希死念慮の判定リストは `src/config/` に分離し、`.gitignore` 済み。 **このファイルの中身をコミットしない**
- 未完了を「失敗」「罰」として表示しない。連続記録を赤字で煽らない

### 定数

卒業判定の定数は**1箇所にまとめる**(`src/constants/graduation.ts`):

```ts
export const GRADUATION_WINDOW = 30;    // 判定窓(日)
export const GRADUATION_THRESHOLD = 21; // 達成日数

```

9月に調整するため、ロジック内に数値を直書きしないこと。

### 秘密情報

コミット禁止: `.env` / `*.p8` / `*.p12` / 証明書 / プロビジョニングプロファイル / RevenueCat Secret Key (`sk_`で始まるもの) ※RevenueCat公開キー(`appl_`)はクライアント埋め込み前提のため可

---

## Git運用ルール

### ブランチ

- `main` を常に動く状態に保つ
- 機能ごとに `feat/today-screen` のようなブランチを切る
- **PRレビューの儀式は不要**(1人開発)。ただしマージ前に実機で動作確認

### コミットメッセージ

Conventional Commits に従う。**英語で書く**(8/29後にリポジトリを公開するため):

```
feat: add routine list persistence
fix: cancel notification when item is checked
docs: update graduation criteria in spec
chore: bump expo-notifications

```

### Issue

**GitHub Issueをタスク管理に使う。**理由は2つ:

1. 何が残っているかを毎回思い出さずに済む
2. **8/29の公開後、issueの履歴がbuild-in-publicの証拠になる**

運用は軽量に:

- 1 issue = 1 まとまった作業(画面1つ、機能1つ)
- ラベルは `v1.0` / `september` の2つだけ。**9月送りの案はissueに落として閉じない**
- コミットメッセージに `#12` を書いて紐付ける
- **issueを増やすことに時間を使わない。** 5分以上かかるなら不要

### 仕様変更のフィードバック

実装中に仕様の判断が変わったら:

1. **同じコミットで** `docs/spec.md` **を更新する**
2. 確度ラベル(【決定】【暫定】【未確定】)を必ず付ける
3. コミットメッセージを `docs:` で始める
4. **却下した案は理由ごと** `docs/spec.md` **の§9に追記する**(最も価値が高い)

実装が進んだだけでは spec.md を更新しない。**判断を記録する場所**であって作業ログではない。

---

## 実装順序

1. データ層(型定義 + AsyncStorageラッパー)
2. Routine setup 画面
3. Today 画面(時系列表示、チェック、永続化)
4. Your KATSU 画面(入力、保存、Tier 1判定)
5. ローカル通知(予約 → 完了でキャンセル)
6. 卒業判定とモーダル
7. RevenueCat(有料App契約の締結待ち)

---

## データモデル

```ts
type RoutineItem = {
  id: string;
  time: string;        // "07:00"
  title: string;
  createdAt: string;   // ISO
};

type DailyRecord = {
  date: string;        // "2026-08-13"
  completedIds: string[];
  totalCount: number;  // その日終了時点のルーティン数(分母を固定するため)
  excludedIds?: string[]; // Day 1で分母から外すと確定した項目。登録時に1回だけ判定し再計算しない
};

type KatsuMessage = {
  id: string;
  text: string;
  routineId?: string;  // 有料のみ。未指定なら全ルーティン共通
};

type AppState = {
  startedOn?: string;   // 最初のルーティンを保存した日 = Day 1
  graduatedAt?: string; // 卒業モーダルを表示済みなら記録(再表示しない)
  onboardedAt?: string; // オンボーディング完了日。startedOn で代用しないこと
};

```

---

## 決定済みの細部(推測で変えないこと)


| 論点                | 決定                                                 |
| ----------------- | -------------------------------------------------- |
| **文言の文字数上限**      | **60文字**(コードポイント単位。`[...str].length` で数える)         |
| **Tier 1判定前の正規化** | `normalize('NFKC')` + ゼロ幅文字除去 + 連続空白圧縮。**保存は元のまま** |
| **通知ペイロード**       | ユーザー本文を入れない。遷移はルーティンIDのみで解決                        |
| **ルーティン登録上限**     | **3件。** 8件では実行率95%でも30日で19.9日にしか届かず卒業できなかった      |
| **完了判定**          | **その日のうちなら何時に押しても達成。** チェック時刻は記録しない                |
| **日付境界**          | 端末のローカル日付。深夜0時で締める。過去日の遡り修正は不可                     |
| 文言が0件のときの通知       | **送らない。** アプリ側の定型文は出さない(核が死ぬため)                    |
| 無料版の通知文言          | 保存できる1つを全ルーティン共通で使う                                |
| 「その日全部完了」の分母      | その日終了時点で存在するルーティン数。過去日は再計算しない                      |
| **Day 1の分母の例外**   | 登録時点で既に時刻が過ぎた項目は、Day 1に限り分母から外す                    |
| **卒業判定**          | **毎日、直近30日のローリングで判定。**Day 30固定チェックではない             |
| 卒業モーダル            | 1回だけ表示。閉じた後もアプリは通常通り使える                            |
| **自主卒業**          | 設定に `I don't need this anymore` を常時置く。同じモーダルを呼ぶ    |
| **課金切れ時**         | 2つ目以降の言葉は**削除せずロック表示**。再課金で復活                      |
| 課金機会              | ①オンボ最後 ②2つ目の言葉 ③出し分け。**それ以外で出さない**                 |
| 通知オフ時             | アプリを開いたときToday画面上部に同じ文言を出す                         |
| ヘルプライン            | 端末ロケールで分岐。国別番号を持たず国際的な案内先に統一                       |
| 通知の実装             | 事前予約 → 完了時にキャンセル(iOSは64件上限)                        |


